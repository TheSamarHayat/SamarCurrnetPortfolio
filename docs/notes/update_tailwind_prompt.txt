I need you to help me upgrade my Tailwind CSS project from version 3.4.19 to 4.3.0. This is a major version upgrade that includes a complete architectural redesign with a Rust-powered engine, CSS-first configuration, new utility classes, and breaking changes. Please follow these steps carefully to ensure a smooth and complete migration.

Start by running the official Tailwind CSS upgrade tool to automate the migration. Execute npx @tailwindcss/upgrade@next in your project root to let the tool update dependencies, migrate configuration files, and handle class name changes. The tool will automatically convert most of your tailwind.config.js to CSS-based @theme directives and rename deprecated utilities like bg-gradient-to-* to bg-linear-to-* and flex-shrink-0 to shrink-0. Review the changes carefully and commit the initial migration before proceeding with manual adjustments.

Next, update your core Tailwind dependencies. Run npm install tailwindcss@4.3.0 @tailwindcss/postcss@4.3.0 @tailwindcss/vite@4.3.0 if using Vite, or @tailwindcss/webpack@4.3.0 if using webpack with Next.js. For webpack-based projects like Next.js, the new @tailwindcss/webpack loader provides over 2x performance improvement compared to PostCSS. Update your PostCSS configuration to use the new @tailwindcss/postcss plugin instead of the old tailwindcss plugin. If you are not using PostCSS, you can skip this step. Check your Node.js version and ensure it's 20 or higher, as Tailwind 4 requires modern Node features.

Handle the CSS import changes in your main CSS file. Replace the old @tailwind directives with a single @import "tailwindcss" statement. Remove @tailwind base, @tailwind components, and @tailwind utilities directives. If your project has any custom layers or configurations, convert them to use the new CSS-first approach with @layer directives where needed. This single import statement now handles all Tailwind CSS generation automatically.

Migrate your configuration from JavaScript to CSS-first @theme format. Open your main CSS file and define your design tokens using @theme with CSS custom properties. For example, define custom colors with --color-brand: #your-color, fonts with --font-sans: "Your Font", and spacing scales with --spacing-{name}. The tool may have already created this, but verify that all your custom theme values from tailwind.config.js are now in the CSS @theme block. Every design token defined in @theme automatically becomes a CSS custom property and generates corresponding utility classes, so bg-brand will work automatically from --color-brand.

Update your build configuration for your specific framework. If using Vite, migrate to the @tailwindcss/vite plugin by updating your vite.config.js to use it instead of PostCSS. If using Next.js with webpack/Turbopack, update your next.config.js to use the @tailwindcss/webpack loader for significant build performance improvements, especially with large projects. For PostCSS-based setups, ensure your postcss.config.js uses @tailwindcss/postcss as the plugin.

Review and apply the breaking changes and utility renaming. The upgrade tool handles most renames automatically, but verify that all your HTML templates use the new canonical class names. Be aware that Tailwind 4 now relies on CSS cascade layers, so if you have other CSS on the page, ensure that non-layered CSS is intentionally styled after Tailwind or properly organized in layers. Check browser support requirements, as Tailwind 4 targets Safari 16.4+, Chrome 111+, Firefox 128+, and will not work in older browsers.

Leverage the new Tailwind 4.3 features in your project. Add scrollbar styling with the new scrollbar-* utilities for track and thumb styling without custom CSS. Use the new @container-size utility alongside container queries for responsive designs that care about both width and height. Implement 3D transforms with rotate-x-*, rotate-y-*, rotate-z-*, scale-z-*, and translate-z-* utilities for modern visual effects. Use the new zoom-* utilities for direct CSS zoom property support. Implement the new tab-* utilities to control tab character width in code blocks. Use color-mix() support in custom colors for dynamic color blending without JavaScript.

Add support for modern CSS features throughout your project. Use @starting-style for smoother entrance animations on elements that are added to the DOM. Implement the new logical property utilities like block-*, inline-*, mbs-*, mbe-*, pbs-*, pbe-* for layouts that work correctly across different writing modes and text directions. Use the new font-features-* utility to control OpenType font features directly from your markup without custom CSS. Use container queries with the @container directive for component-level responsive design.

Take advantage of the new color palettes. Add the four new neutral color palettes—mauve, olive, mist, and taupe—to your design where appropriate for warmer, cooler, greener, or more sophisticated neutral tones. Use these in place of existing gray, zinc, neutral, stone, or slate palettes where you need subtle personality variations. Update your color tokens in @theme to include these new palettes for comprehensive design system coverage.

Verify your PostCSS configuration and external CSS integration. If you have external CSS files or third-party stylesheets, ensure they are properly imported and respect the new cascade layer system. Test that your custom CSS doesn't interfere with Tailwind's generated utilities by understanding how layers work in CSS cascade. Add any custom layers if needed using @layer directives in your CSS.

Test all components and pages thoroughly in the browser to verify that the migration is complete and correct. Check that all utility classes are working as expected, paying special attention to renamed classes and any custom theme extensions. Verify responsive behavior, dark mode styling, and hover/focus states. Test on the modern browsers that Tailwind 4 supports—Safari 16.4+, Chrome 111+, Firefox 128+—and note that support for older browsers is not available in this version.

Run your build process and verify build times have improved. You should see 2-5x faster builds compared to v3, and the incremental builds should be significantly faster. If using Next.js with the webpack loader, expect even more dramatic improvements over the PostCSS approach. Measure and document the performance gains to confirm the upgrade benefits.

Finally, review the official Tailwind CSS 4.3 documentation at https://tailwindcss.com/docs for any additional features or configuration options specific to your use case. Check the full list of renamed utilities and breaking changes if you encounter any issues during testing. Test the entire application flow end-to-end in a staging environment before deploying to production. Pay special attention to any custom components, third-party integrations, and critical user flows to ensure everything works correctly with the new Tailwind version.

