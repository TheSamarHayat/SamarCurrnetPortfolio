I need you to help me upgrade my Next.js project from version 15.5.9 to 16.2.7. This is a major version upgrade that includes significant changes in React, bundler, caching strategy, and APIs. Please follow these steps carefully to ensure a smooth and complete migration.

Start by updating all core dependencies. Run npm install next@16.2.7 react@19.2.0 react-dom@19.2.0 to update to the latest stable versions. Also update TypeScript to version 5.3 or higher and ensure @types/react and @types/react-dom are at their latest versions. Check that your Node.js version is 20.9 or higher, as Node.js 18 is no longer supported. If you have a package-lock.json file, delete it and run npm install to regenerate it with the new versions.

Next, handle the Turbopack configuration changes. Since Turbopack is now the default bundler in Next.js 16, you should remove any --turbopack or --turbo flags from your package.json scripts if they exist. If your next.config.js has experimental.turbopack configuration, move it to the top level as a turbopack property. Check if your project uses any custom webpack configurations, and if so, either migrate them to Turbopack-compatible options or use the --webpack flag to opt out. For most projects, using Turbopack by default provides 2-5x faster builds and 10x faster Fast Refresh without any configuration changes.

Now address the breaking changes for Async Request APIs. Starting with Next.js 16, all dynamic route parameters and search parameters must be accessed asynchronously. Find all instances where you access params or searchParams synchronously in layout.js, page.js, route.js, default.js, opengraph-image, twitter-image, icon, and apple-icon files. Change these from functions that receive params as a direct object to async functions that await params. For example, change export function generateImageMetadata({ params }) to export async function generateImageMetadata({ params }) and then await params before destructuring. Run npx next codemod@canary upgrade to automatically apply most of these changes, but verify the results manually.

Implement the new caching strategy using the updated APIs. Replace any ISR (Incremental Static Regeneration) logic with the new explicit caching methods: use revalidateTag() for tag-based revalidation, updateTag() for updating specific cache tags, and refresh() for manual cache clearing. Review all your data fetching patterns and update cache configurations accordingly. The new use cache directive can be added to server components to specify caching behavior inline, which replaces the need for complex revalidation strategies.

Implement Cache Components if your application benefits from fine-grained caching. Cache Components use Partial Pre-Rendering to deliver instant perceived navigation by serving static HTML shells with dynamically streamed content. Review routes where you have long-running async operations and wrap those components with the Cache Component pattern to improve perceived performance.

Add support for View Transitions for smoother page navigation. Import useTransition from react and use it in your navigation components to enable animated transitions between pages. This uses the browser's native View Transition API and requires no external animation libraries. Update your Link components and client-side navigation to integrate this feature for better user experience.

Leverage new React 19 features throughout your application. Use the useEffectEvent hook to extract non-reactive logic from effects and avoid stale closure problems. If applicable to your use case, configure the React Compiler by setting experimental.reactCompiler: true in your next.config.js to automatically optimize your components. Review and refactor any complex hooks patterns that can benefit from useEffectEvent.

Update your error handling and debugging approach. Next.js 16.2 includes a redesigned default error page with better visual clarity. Review any custom error pages you've created and ensure they align with the new error page design. Take advantage of the new hydration diff indicator which clearly shows server-side vs client-side rendering differences in the error overlay during development. Use the new --inspect flag for next start to attach the Node.js debugger to your production server if you need to debug production issues.

Optimize server-side logging for development. The new Server Function Logging feature logs server function execution directly to your development terminal, making debugging Server Actions easier. Review your Server Action implementations and add appropriate logging to understand execution flow and data flow in development.

Test all authentication flows and middleware carefully. If you're using middleware, ensure it's updated to work with the new Request handling in Next.js 16. Check Auth.js v5 compatibility if you're using authentication. Test login, logout, and protected routes thoroughly to ensure session management works correctly.

Validate all API routes and Server Actions are working as expected. Test error scenarios, edge cases, and ensure proper error handling with the new error pages. Check that any error boundaries are functioning correctly with React 19.

Run your test suite if you have one, ensuring all tests pass with the new versions. Update any tests that mock Next.js internals or test framework-specific behavior, as some APIs may have changed.

Verify performance improvements in your application. Measure and compare build times before and after, noting the significant improvements Turbopack should provide. Check rendering performance in your browser DevTools and compare with previous versions to validate the rendering performance improvements.

Finally, review the official Next.js 16 migration guide at https://nextjs.org/docs/app/guides/upgrading/version-16 for any additional changes specific to your setup. Test the entire application flow end-to-end in a staging environment before deploying to production. Pay special attention to any custom configurations, third-party integrations, and critical user flows to ensure everything works as expected in the new version.

